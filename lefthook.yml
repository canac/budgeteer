pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm biome check --write --unsafe --error-on-warnings --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
